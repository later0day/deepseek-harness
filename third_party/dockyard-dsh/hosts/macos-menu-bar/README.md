# Dockyard DSH macOS Host

The menu bar is a host module only. It may discover user-approved local OAuth states, display live account/quota/refresh data, and send commands to the Dockyard runtime.

It must not contain provider-specific OAuth logic or implement account selection itself.
